# Marathon_BIB_Detector_Application
This is tested and working python application for detecting and reading the bib numbers on marathon runners dataset. The application uses a pre-trained YOLO model trained on bib numbers dataset. The code is written compatible for docker container.
